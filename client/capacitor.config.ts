import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.cottoncandyeventdeco.admin',
  appName: 'Cotton Candy Admin',
  webDir: 'dist'
};

export default config;
